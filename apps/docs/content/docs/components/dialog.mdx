---
title: Dialog
description: A modal dialog built on React Native's Modal, with a backdrop and focus containment.
---

`Dialog` is a modal overlay. Its `Portal` is a React Native `Modal`, so the popup renders above the
app with focus containment and Android back-button dismissal handled for you.

<Demo>Opening and dismissing a dialog.</Demo>

## Anatomy

<Anatomy code={`Dialog.Root
  Dialog.Trigger
  Dialog.Portal
    Dialog.Backdrop
    Dialog.Viewport
      Dialog.Popup
        Dialog.Title
        Dialog.Description
        Dialog.Close`} />

## Usage

The behaviour is headless — the **Code** tab is the component; the **StyleSheet** and **Uniwind** tabs are two ways to design it.

<Tabs items={['Code', 'StyleSheet', 'Uniwind']}>

<Tab value="Code">

```tsx
import { Text } from 'react-native';
import { Dialog } from '@limonify/zest';

<Dialog.Root>
  <Dialog.Trigger style={styles.button}>
    <Text style={styles.buttonText}>Delete</Text>
  </Dialog.Trigger>

  <Dialog.Portal>
    <Dialog.Backdrop style={styles.backdrop} />
    <Dialog.Viewport style={styles.viewport}>
      <Dialog.Popup style={styles.card}>
        <Dialog.Title style={styles.title}>Delete file?</Dialog.Title>
        <Dialog.Description style={styles.body}>This can't be undone.</Dialog.Description>
        <Dialog.Close style={styles.button}>
          <Text style={styles.buttonText}>Cancel</Text>
        </Dialog.Close>
      </Dialog.Popup>
    </Dialog.Viewport>
  </Dialog.Portal>
</Dialog.Root>
```

</Tab>

<Tab value="StyleSheet">

```tsx
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  button: {
    alignSelf: 'flex-start',
    backgroundColor: '#171717',
    borderRadius: 10,
    paddingVertical: 10,
    paddingHorizontal: 18,
  },
  buttonText: { color: '#fff', fontWeight: '600' },
  backdrop: { ...StyleSheet.absoluteFillObject, backgroundColor: 'rgba(0,0,0,0.5)' },
  viewport: { flex: 1, alignItems: 'center', justifyContent: 'center', padding: 24 },
  card: { width: '100%', maxWidth: 340, gap: 8, borderRadius: 16, backgroundColor: '#fff', padding: 20 },
  title: { fontSize: 17, fontWeight: '700' },
  body: { color: '#666' },
});
```

</Tab>

<Tab value="Uniwind">

```tsx
import { Text } from 'react-native';
import { Dialog } from '@limonify/zest';

<Dialog.Root>
  <Dialog.Trigger className="self-start rounded-lg bg-neutral-900 px-4 py-2.5">
    <Text className="font-semibold text-white">Delete</Text>
  </Dialog.Trigger>

  <Dialog.Portal>
    <Dialog.Backdrop className="absolute inset-0 bg-black/50" />
    <Dialog.Viewport className="flex-1 items-center justify-center p-6">
      <Dialog.Popup className="w-full max-w-[340px] gap-2 rounded-2xl bg-white p-5">
        <Dialog.Title className="text-[17px] font-bold">Delete file?</Dialog.Title>
        <Dialog.Description className="text-neutral-500">This can't be undone.</Dialog.Description>
        <Dialog.Close className="self-start rounded-lg bg-neutral-900 px-4 py-2.5">
          <Text className="font-semibold text-white">Cancel</Text>
        </Dialog.Close>
      </Dialog.Popup>
    </Dialog.Viewport>
  </Dialog.Portal>
</Dialog.Root>
```

</Tab>

</Tabs>

## Dismissal

The `Backdrop` (or a press outside the `Popup` in the `Viewport`) closes the dialog with the
`outside-press` reason; the Android back button and the OS close it with `escape-key`. Every close
reports through `onOpenChange(open, eventDetails)` so you can veto it with `eventDetails.cancel()`.

## API

`Dialog.Root`
<AutoType path="../../packages/zest/src/dialog/root/DialogRoot.tsx" name="DialogRootProps" />


## State

These fields reach your `style` and `render` functions on **Dialog.Popup** — the React Native counterpart of Base UI's `data-*` attributes (`style={(state) => ...}`).

<AutoType path="../../packages/zest/src/dialog/popup/DialogPopup.tsx" name="DialogPopupState" />

## Accessibility

The popup is `accessibilityViewIsModal` with `accessibilityLabelledBy` pointing at `Dialog.Title`
and `accessibilityDescribedBy` at `Dialog.Description`. Unlike the web version there is no
`aria-modal`-only fallback — the RN `Modal` contains focus natively.
