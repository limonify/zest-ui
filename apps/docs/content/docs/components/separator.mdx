---
title: Separator
description: A visual and semantic divider.
---

`Separator` draws a divider between content, with the right accessibility role and orientation.

<Demo>A horizontal and a vertical separator.</Demo>

## Anatomy

<Anatomy code={`Separator`} />

## Usage

```tsx
import { Separator } from '@limonify/zest';

<Separator orientation="horizontal" style={styles.divider} />
```

## API

`Separator` — root props below. Every part also accepts the [common props](/docs/philosophy).

<AutoType path="../../packages/zest/src/separator/Separator.tsx" name="SeparatorProps" />

## State

These fields reach your `style` and `render` functions on **Separator** — the React Native counterpart of Base UI's `data-*` attributes (`style={(state) => ...}`).

<AutoType path="../../packages/zest/src/separator/Separator.tsx" name="SeparatorState" />

## Accessibility

`accessibilityRole` maps to a separator; `orientation` sets `aria-orientation`.
