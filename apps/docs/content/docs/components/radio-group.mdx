---
title: Radio Group
description: Shared state for a set of radio buttons.
---

`RadioGroup` provides the selected value to a set of `Radio` buttons and commits changes before applying them, so a handler can veto.

## Usage

Press it, then read it. The behaviour is headless — the **Code** tab is the component; the **StyleSheet** and **Uniwind** tabs are two ways to design it.

<Tabs items={['Preview', 'Code', 'StyleSheet', 'Uniwind']}>

<Tab value="Preview">

<Demo name="radio-group" />

</Tab>

<Tab value="Code">

```tsx
import { RadioGroup, Radio } from '@limonify/zest-ui';

<RadioGroup defaultValue="md" onValueChange={setSize} style={styles.group}>
  {/* Radio.Root items */}
</RadioGroup>
```

</Tab>

<Tab value="StyleSheet">

```tsx
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  group: { gap: 4 },
});
```

</Tab>

<Tab value="Uniwind">

```tsx
import { RadioGroup } from '@limonify/zest-ui';

<RadioGroup defaultValue="md" onValueChange={setSize} className="gap-1">
  {/* Radio.Root items */}
</RadioGroup>
```

</Tab>

</Tabs>


## Anatomy

<Anatomy code={`RadioGroup
  Radio.Root`} />

## State

These fields reach your `style` and `render` functions on **RadioGroup** (`style={(state) => ...}`).

<AutoType path="../../packages/zest-ui/src/radio-group/RadioGroup.tsx" name="RadioGroupState" />

## Accessibility

`accessibilityRole="radiogroup"`. Inside a `Field.Root`, the group is labelled and validated by the field.

## API

`Radio Group` — the props below sit on the root. Every part also accepts the [common props](/docs/common-props) (`style`/`className` as a value or function of state, `render`, `ref`).

<AutoType path="../../packages/zest-ui/src/radio-group/RadioGroup.tsx" name="RadioGroupProps" />
